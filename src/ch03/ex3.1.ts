function squareOf(n: number) {
  return n * n;
}

function greeting(str: string) {
  return `Hello, ${str}`;
}

squareOf(2);
// squareOf('z');
// squareOf(undefined);

greeting('John');
// greeting(9);
// greeting(null);

export {};
